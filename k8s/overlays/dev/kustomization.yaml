resources:
  - ../../base
  - configmap.yaml
  #- app-servicemonitor.yaml

namespace: dev

patches:
  - target:
      kind: Deployment
      name: app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
