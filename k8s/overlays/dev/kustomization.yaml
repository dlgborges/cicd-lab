resources:
  - ../../base
  - configmap.yaml
  - app-servicemonitor.yaml

namespace: dev

patches:
  - target:
      kind: Deployment
      name: nginx
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
