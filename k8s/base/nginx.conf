events {}

http {
    server {
        listen 80;

        location / {
            return 200 "Hello from nginx\n";
        }

        location /stub_status {
            stub_status;
            allow all;
        }
    }
}