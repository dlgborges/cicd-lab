apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
    release: monitoring
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
