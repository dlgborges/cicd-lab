apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
    release: monitoring
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: metrics
      port: 9113
      targetPort: 9113
