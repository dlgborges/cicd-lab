apiVersion: v1
kind: Service
metadata:
  name: app
  labels:
    app: app
    release: monitoring
spec:
  type: NodePort
  selector:
    app: app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
